# Demo Environment Configuration
# Copy this to .env.local for local development

# Required for demo deployment
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://your-demo-app.vercel.app

# Demo Authentication (Generate: openssl rand -base64 32)
AUTH_SECRET=demo-secret-key-replace-with-32-chars-minimum-length-required

# Demo Database (Use Vercel Postgres free tier)
POSTGRES_URL=postgresql://username:password@hostname:port/database

# AI Service (Required - Get from OpenAI)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Azure Search (Leave empty for demo without search)
# AZURE_SEARCH_ENDPOINT=https://your-search.search.windows.net
# AZURE_SEARCH_KEY=your-search-key

# Demo Client Configuration
DEFAULT_CLIENT_ID=demo-client

# Optional: Analytics
# VERCEL_ANALYTICS_ID=your-analytics-id